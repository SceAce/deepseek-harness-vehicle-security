interface:
  display_name: "Solve CTF Web"
  short_description: "Tool-first web challenge workflow"
  default_prompt: "Use $solve-ctf-web, call ctf_tool_audit first, prefer mcp-chrome for interactive browser state and Tavily for CVE/version research, then compare request diffs before writing custom web code."

dependencies:
  tools:
    - type: "mcp"
      value: "ctf-security"
      description: "Portable deterministic CTF tools"
      transport: "stdio"
