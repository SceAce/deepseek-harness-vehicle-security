interface:
  display_name: "Solve CTF RE"
  short_description: "Tool-first reverse-engineering CTF workflow"
  default_prompt: "Use $solve-ctf-re. Inspect ctf_tool_audit/toolBindings when capability state is unknown, then choose ctf_re_profile, ctf_re_r2_query, mcp.ida_pro, or ctf_re_ida_script according to the evidence question. Treat nextActions and the tool graph as recommendations, not a fixed order."

dependencies:
  tools:
    - type: "mcp"
      value: "ctf-security"
      description: "Portable deterministic CTF tools"
      transport: "stdio"
