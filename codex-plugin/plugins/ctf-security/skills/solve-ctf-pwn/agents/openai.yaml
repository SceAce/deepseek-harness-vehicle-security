interface:
  display_name: "Solve CTF Pwn"
  short_description: "Tool-first binary exploitation workflow"
  default_prompt: "Use $solve-ctf-pwn, call ctf_tool_audit first, prefer GDB/Pwndbg MCP or local Pwndbg, then run debugger and gadget tools before writing an exploit."

dependencies:
  tools:
    - type: "mcp"
      value: "ctf-security"
      description: "Portable deterministic CTF tools"
      transport: "stdio"
